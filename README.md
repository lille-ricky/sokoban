
### Pour compiler l'affichage en texte :
Depuis le repertoire ```sokoban```:
```  javac -d bin -sourcepath src src/modele/*.java src/vueTexte/*.java ```\
Pour lancer le jeu : ```java -cp bin vueTexte.SokobanTexte```
